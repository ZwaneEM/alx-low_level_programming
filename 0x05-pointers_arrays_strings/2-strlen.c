#include "main.h"

/**
 * _strlen - prints a string of text
 * Return: Returns void
 * @s: describe the parameter
 */
int _strlen(char *s)
{
	int i;
	int hold;

	for (i = 0; s[i] != '\0'; i++)
	{
		hold = i + 1;
	}
	return (hold);

}
