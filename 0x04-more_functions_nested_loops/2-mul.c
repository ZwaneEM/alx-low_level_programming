#include <stdio.h>

/**
 * mul - multiples two digits
 * Return: Returns mul
 * @a : a value of a character is passed
 * @b : a value of a character is passed
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
