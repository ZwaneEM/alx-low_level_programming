#include "main.h"

/**
 * print_alphabet - prints a string of text
 * Return: Returns a 0
 */
void print_alphabet(void)
{
	int i;

	for (i = 0; i <= 25; i++)
	{
		_putchar('a' + i);
	}
	_putchar('\n');

	/*return (0);*/
}
